rds:
watchers-db
Fin2NumbersNoSymbol
endpoint - watchers-db.cluosayiqd4n.il-central-1.rds.amazonaws.com
port 3306


su - watchers 2*FinNumSym
u - bejerano

Docker:
docker build -t watchers .
docker build --platform=linux/amd64/v4 -t watchers_aws .
docker run -d -p 8010:8000 --name watchers-container watchers
docker save watchers -o watchers.tar

EC2:
ssh -i ../aws/ec2-tlv-general.pem ec2-user@51.17.183.233
scp -i ../aws/ec2-tlv-general.pem watchers.tar ec2-user@51.17.183.233:/home/ec2-user/docker_images

docker load -i watchers.tar

the script
1. build the docker for aws:
  docker build --no-cache --platform=linux/amd64/v4 -t watchers_aws .
2. save the docker:
  docker save watchers_aws -o watchers_aws.tar
3. copy the docker to the ec2:
  scp -i ../aws/ec2-tlv-general.pem watchers_aws.tar ec2-user@51.17.183.233:/home/ec2-user/docker_images
4. delete the image:
  rm watchers_aws.tar
4. ssh to the ec2:
  ssh -i ../aws/ec2-tlv-general.pem ec2-user@51.17.183.233:/docker_images
5. load the docker:
  docker load -i watchers_aws.tar
6. run the docker:
  docker run -d -p 8010:8000 --name watchers watchers_aws

